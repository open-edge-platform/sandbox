// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/open-edge-platform/infra-core/inventory/v2/internal/ent/ouresource"
	"github.com/open-edge-platform/infra-core/inventory/v2/internal/ent/predicate"
	"github.com/open-edge-platform/infra-core/inventory/v2/internal/ent/providerresource"
	"github.com/open-edge-platform/infra-core/inventory/v2/internal/ent/regionresource"
	"github.com/open-edge-platform/infra-core/inventory/v2/internal/ent/siteresource"
)

// SiteResourceQuery is the builder for querying SiteResource entities.
type SiteResourceQuery struct {
	config
	ctx          *QueryContext
	order        []siteresource.OrderOption
	inters       []Interceptor
	predicates   []predicate.SiteResource
	withRegion   *RegionResourceQuery
	withOu       *OuResourceQuery
	withProvider *ProviderResourceQuery
	withFKs      bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SiteResourceQuery builder.
func (srq *SiteResourceQuery) Where(ps ...predicate.SiteResource) *SiteResourceQuery {
	srq.predicates = append(srq.predicates, ps...)
	return srq
}

// Limit the number of records to be returned by this query.
func (srq *SiteResourceQuery) Limit(limit int) *SiteResourceQuery {
	srq.ctx.Limit = &limit
	return srq
}

// Offset to start from.
func (srq *SiteResourceQuery) Offset(offset int) *SiteResourceQuery {
	srq.ctx.Offset = &offset
	return srq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (srq *SiteResourceQuery) Unique(unique bool) *SiteResourceQuery {
	srq.ctx.Unique = &unique
	return srq
}

// Order specifies how the records should be ordered.
func (srq *SiteResourceQuery) Order(o ...siteresource.OrderOption) *SiteResourceQuery {
	srq.order = append(srq.order, o...)
	return srq
}

// QueryRegion chains the current query on the "region" edge.
func (srq *SiteResourceQuery) QueryRegion() *RegionResourceQuery {
	query := (&RegionResourceClient{config: srq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := srq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := srq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(siteresource.Table, siteresource.FieldID, selector),
			sqlgraph.To(regionresource.Table, regionresource.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, siteresource.RegionTable, siteresource.RegionColumn),
		)
		fromU = sqlgraph.SetNeighbors(srq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOu chains the current query on the "ou" edge.
func (srq *SiteResourceQuery) QueryOu() *OuResourceQuery {
	query := (&OuResourceClient{config: srq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := srq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := srq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(siteresource.Table, siteresource.FieldID, selector),
			sqlgraph.To(ouresource.Table, ouresource.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, siteresource.OuTable, siteresource.OuColumn),
		)
		fromU = sqlgraph.SetNeighbors(srq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryProvider chains the current query on the "provider" edge.
func (srq *SiteResourceQuery) QueryProvider() *ProviderResourceQuery {
	query := (&ProviderResourceClient{config: srq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := srq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := srq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(siteresource.Table, siteresource.FieldID, selector),
			sqlgraph.To(providerresource.Table, providerresource.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, siteresource.ProviderTable, siteresource.ProviderColumn),
		)
		fromU = sqlgraph.SetNeighbors(srq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SiteResource entity from the query.
// Returns a *NotFoundError when no SiteResource was found.
func (srq *SiteResourceQuery) First(ctx context.Context) (*SiteResource, error) {
	nodes, err := srq.Limit(1).All(setContextOp(ctx, srq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{siteresource.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (srq *SiteResourceQuery) FirstX(ctx context.Context) *SiteResource {
	node, err := srq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SiteResource ID from the query.
// Returns a *NotFoundError when no SiteResource ID was found.
func (srq *SiteResourceQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = srq.Limit(1).IDs(setContextOp(ctx, srq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{siteresource.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (srq *SiteResourceQuery) FirstIDX(ctx context.Context) int {
	id, err := srq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SiteResource entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SiteResource entity is found.
// Returns a *NotFoundError when no SiteResource entities are found.
func (srq *SiteResourceQuery) Only(ctx context.Context) (*SiteResource, error) {
	nodes, err := srq.Limit(2).All(setContextOp(ctx, srq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{siteresource.Label}
	default:
		return nil, &NotSingularError{siteresource.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (srq *SiteResourceQuery) OnlyX(ctx context.Context) *SiteResource {
	node, err := srq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SiteResource ID in the query.
// Returns a *NotSingularError when more than one SiteResource ID is found.
// Returns a *NotFoundError when no entities are found.
func (srq *SiteResourceQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = srq.Limit(2).IDs(setContextOp(ctx, srq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{siteresource.Label}
	default:
		err = &NotSingularError{siteresource.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (srq *SiteResourceQuery) OnlyIDX(ctx context.Context) int {
	id, err := srq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SiteResources.
func (srq *SiteResourceQuery) All(ctx context.Context) ([]*SiteResource, error) {
	ctx = setContextOp(ctx, srq.ctx, ent.OpQueryAll)
	if err := srq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SiteResource, *SiteResourceQuery]()
	return withInterceptors[[]*SiteResource](ctx, srq, qr, srq.inters)
}

// AllX is like All, but panics if an error occurs.
func (srq *SiteResourceQuery) AllX(ctx context.Context) []*SiteResource {
	nodes, err := srq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SiteResource IDs.
func (srq *SiteResourceQuery) IDs(ctx context.Context) (ids []int, err error) {
	if srq.ctx.Unique == nil && srq.path != nil {
		srq.Unique(true)
	}
	ctx = setContextOp(ctx, srq.ctx, ent.OpQueryIDs)
	if err = srq.Select(siteresource.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (srq *SiteResourceQuery) IDsX(ctx context.Context) []int {
	ids, err := srq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (srq *SiteResourceQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, srq.ctx, ent.OpQueryCount)
	if err := srq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, srq, querierCount[*SiteResourceQuery](), srq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (srq *SiteResourceQuery) CountX(ctx context.Context) int {
	count, err := srq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (srq *SiteResourceQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, srq.ctx, ent.OpQueryExist)
	switch _, err := srq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (srq *SiteResourceQuery) ExistX(ctx context.Context) bool {
	exist, err := srq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SiteResourceQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (srq *SiteResourceQuery) Clone() *SiteResourceQuery {
	if srq == nil {
		return nil
	}
	return &SiteResourceQuery{
		config:       srq.config,
		ctx:          srq.ctx.Clone(),
		order:        append([]siteresource.OrderOption{}, srq.order...),
		inters:       append([]Interceptor{}, srq.inters...),
		predicates:   append([]predicate.SiteResource{}, srq.predicates...),
		withRegion:   srq.withRegion.Clone(),
		withOu:       srq.withOu.Clone(),
		withProvider: srq.withProvider.Clone(),
		// clone intermediate query.
		sql:  srq.sql.Clone(),
		path: srq.path,
	}
}

// WithRegion tells the query-builder to eager-load the nodes that are connected to
// the "region" edge. The optional arguments are used to configure the query builder of the edge.
func (srq *SiteResourceQuery) WithRegion(opts ...func(*RegionResourceQuery)) *SiteResourceQuery {
	query := (&RegionResourceClient{config: srq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	srq.withRegion = query
	return srq
}

// WithOu tells the query-builder to eager-load the nodes that are connected to
// the "ou" edge. The optional arguments are used to configure the query builder of the edge.
func (srq *SiteResourceQuery) WithOu(opts ...func(*OuResourceQuery)) *SiteResourceQuery {
	query := (&OuResourceClient{config: srq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	srq.withOu = query
	return srq
}

// WithProvider tells the query-builder to eager-load the nodes that are connected to
// the "provider" edge. The optional arguments are used to configure the query builder of the edge.
func (srq *SiteResourceQuery) WithProvider(opts ...func(*ProviderResourceQuery)) *SiteResourceQuery {
	query := (&ProviderResourceClient{config: srq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	srq.withProvider = query
	return srq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ResourceID string `json:"resource_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SiteResource.Query().
//		GroupBy(siteresource.FieldResourceID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (srq *SiteResourceQuery) GroupBy(field string, fields ...string) *SiteResourceGroupBy {
	srq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SiteResourceGroupBy{build: srq}
	grbuild.flds = &srq.ctx.Fields
	grbuild.label = siteresource.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ResourceID string `json:"resource_id,omitempty"`
//	}
//
//	client.SiteResource.Query().
//		Select(siteresource.FieldResourceID).
//		Scan(ctx, &v)
func (srq *SiteResourceQuery) Select(fields ...string) *SiteResourceSelect {
	srq.ctx.Fields = append(srq.ctx.Fields, fields...)
	sbuild := &SiteResourceSelect{SiteResourceQuery: srq}
	sbuild.label = siteresource.Label
	sbuild.flds, sbuild.scan = &srq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SiteResourceSelect configured with the given aggregations.
func (srq *SiteResourceQuery) Aggregate(fns ...AggregateFunc) *SiteResourceSelect {
	return srq.Select().Aggregate(fns...)
}

func (srq *SiteResourceQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range srq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, srq); err != nil {
				return err
			}
		}
	}
	for _, f := range srq.ctx.Fields {
		if !siteresource.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if srq.path != nil {
		prev, err := srq.path(ctx)
		if err != nil {
			return err
		}
		srq.sql = prev
	}
	return nil
}

func (srq *SiteResourceQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SiteResource, error) {
	var (
		nodes       = []*SiteResource{}
		withFKs     = srq.withFKs
		_spec       = srq.querySpec()
		loadedTypes = [3]bool{
			srq.withRegion != nil,
			srq.withOu != nil,
			srq.withProvider != nil,
		}
	)
	if srq.withRegion != nil || srq.withOu != nil || srq.withProvider != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, siteresource.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SiteResource).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SiteResource{config: srq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, srq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := srq.withRegion; query != nil {
		if err := srq.loadRegion(ctx, query, nodes, nil,
			func(n *SiteResource, e *RegionResource) { n.Edges.Region = e }); err != nil {
			return nil, err
		}
	}
	if query := srq.withOu; query != nil {
		if err := srq.loadOu(ctx, query, nodes, nil,
			func(n *SiteResource, e *OuResource) { n.Edges.Ou = e }); err != nil {
			return nil, err
		}
	}
	if query := srq.withProvider; query != nil {
		if err := srq.loadProvider(ctx, query, nodes, nil,
			func(n *SiteResource, e *ProviderResource) { n.Edges.Provider = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (srq *SiteResourceQuery) loadRegion(ctx context.Context, query *RegionResourceQuery, nodes []*SiteResource, init func(*SiteResource), assign func(*SiteResource, *RegionResource)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*SiteResource)
	for i := range nodes {
		if nodes[i].site_resource_region == nil {
			continue
		}
		fk := *nodes[i].site_resource_region
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(regionresource.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "site_resource_region" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (srq *SiteResourceQuery) loadOu(ctx context.Context, query *OuResourceQuery, nodes []*SiteResource, init func(*SiteResource), assign func(*SiteResource, *OuResource)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*SiteResource)
	for i := range nodes {
		if nodes[i].site_resource_ou == nil {
			continue
		}
		fk := *nodes[i].site_resource_ou
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(ouresource.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "site_resource_ou" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (srq *SiteResourceQuery) loadProvider(ctx context.Context, query *ProviderResourceQuery, nodes []*SiteResource, init func(*SiteResource), assign func(*SiteResource, *ProviderResource)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*SiteResource)
	for i := range nodes {
		if nodes[i].site_resource_provider == nil {
			continue
		}
		fk := *nodes[i].site_resource_provider
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(providerresource.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "site_resource_provider" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (srq *SiteResourceQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := srq.querySpec()
	_spec.Node.Columns = srq.ctx.Fields
	if len(srq.ctx.Fields) > 0 {
		_spec.Unique = srq.ctx.Unique != nil && *srq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, srq.driver, _spec)
}

func (srq *SiteResourceQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(siteresource.Table, siteresource.Columns, sqlgraph.NewFieldSpec(siteresource.FieldID, field.TypeInt))
	_spec.From = srq.sql
	if unique := srq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if srq.path != nil {
		_spec.Unique = true
	}
	if fields := srq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, siteresource.FieldID)
		for i := range fields {
			if fields[i] != siteresource.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := srq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := srq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := srq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := srq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (srq *SiteResourceQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(srq.driver.Dialect())
	t1 := builder.Table(siteresource.Table)
	columns := srq.ctx.Fields
	if len(columns) == 0 {
		columns = siteresource.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if srq.sql != nil {
		selector = srq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if srq.ctx.Unique != nil && *srq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range srq.predicates {
		p(selector)
	}
	for _, p := range srq.order {
		p(selector)
	}
	if offset := srq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := srq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SiteResourceGroupBy is the group-by builder for SiteResource entities.
type SiteResourceGroupBy struct {
	selector
	build *SiteResourceQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (srgb *SiteResourceGroupBy) Aggregate(fns ...AggregateFunc) *SiteResourceGroupBy {
	srgb.fns = append(srgb.fns, fns...)
	return srgb
}

// Scan applies the selector query and scans the result into the given value.
func (srgb *SiteResourceGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, srgb.build.ctx, ent.OpQueryGroupBy)
	if err := srgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SiteResourceQuery, *SiteResourceGroupBy](ctx, srgb.build, srgb, srgb.build.inters, v)
}

func (srgb *SiteResourceGroupBy) sqlScan(ctx context.Context, root *SiteResourceQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(srgb.fns))
	for _, fn := range srgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*srgb.flds)+len(srgb.fns))
		for _, f := range *srgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*srgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := srgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SiteResourceSelect is the builder for selecting fields of SiteResource entities.
type SiteResourceSelect struct {
	*SiteResourceQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (srs *SiteResourceSelect) Aggregate(fns ...AggregateFunc) *SiteResourceSelect {
	srs.fns = append(srs.fns, fns...)
	return srs
}

// Scan applies the selector query and scans the result into the given value.
func (srs *SiteResourceSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, srs.ctx, ent.OpQuerySelect)
	if err := srs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SiteResourceQuery, *SiteResourceSelect](ctx, srs.SiteResourceQuery, srs, srs.inters, v)
}

func (srs *SiteResourceSelect) sqlScan(ctx context.Context, root *SiteResourceQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(srs.fns))
	for _, fn := range srs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*srs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := srs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
