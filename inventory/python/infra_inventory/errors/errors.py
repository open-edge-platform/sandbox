# Generated by the protocol buffer compiler.  DO NOT EDIT!
# sources: errors/errors.proto
# plugin: python-betterproto
from dataclasses import dataclass
from typing import List

import betterproto

from .google import protobuf


class Reason(betterproto.Enum):
    """
    These are our error codes, meant to be processed by machines or programs.
    Not really useful for humans.
    """

    # First value must be 0 and specified
    OK = 0
    # UNKNOWN_CLIENT means client is unknown to the server and a new registration
    # must be re-issued
    UNKNOWN_CLIENT = 40
    # OPERATION_IN_PROGRESS means that some action cannot be performed because
    # there is other operation on a given resource in progress.
    OPERATION_IN_PROGRESS = 41


@dataclass
class ErrorInfo(betterproto.Message):
    # The reason of the error. This is a constant value that identifies the
    # proximate cause of the error. Error reasons are unique within a particular
    # domain of errors.
    reason: "Reason" = betterproto.enum_field(1)
    # The full error stack. Including the linenumber from which the error
    # originated. This might require a wrap before generating this error info if
    # the error is coming from a package outside our code. This information is
    # only for internal debugging and not meant to be shared outside.
    stacktrace: str = betterproto.string_field(2)
    # A list of messages that carry additional error details to be standardized
    # within this file.
    details: List[protobuf.Any] = betterproto.message_field(3)
