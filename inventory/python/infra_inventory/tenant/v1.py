# Generated by the protocol buffer compiler.  DO NOT EDIT!
# sources: tenant/v1/tenant.proto
# plugin: python-betterproto
from dataclasses import dataclass

import betterproto


class TenantState(betterproto.Enum):
    """An Enum with the states defined by the Multi-tenant framework"""

    TENANT_STATE_UNSPECIFIED = 0
    TENANT_STATE_CREATED = 1
    TENANT_STATE_DELETED = 2


@dataclass
class Tenant(betterproto.Message):
    # resource identifier
    resource_id: str = betterproto.string_field(1)
    # Expresses current state of tenant.
    current_state: "TenantState" = betterproto.enum_field(2)
    # Expresses desired state of tenant.
    desired_state: "TenantState" = betterproto.enum_field(3)
    # state of tenant initialization on osmanager side
    watcher_osmanager: bool = betterproto.bool_field(4)
    tenant_id: str = betterproto.string_field(100)
    created_at: str = betterproto.string_field(200)
    updated_at: str = betterproto.string_field(201)
