import { tooltip, tooltipBase } from './component';
import { modes } from './modes';
import { properties } from './properties';
import { TooltipPlacement, TooltipSize } from './types';
export { tooltipBase, TooltipPlacement, TooltipSize };
export const config = {
    properties,
    component: tooltip,
    modes
};
