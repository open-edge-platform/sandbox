import { token } from '../../setup';
export const prefix = 'spark-breadcrumb';
export const properties = token({
    splitGap: '0.38rem',
    margin: 0,
    padding: 0,
    blockSize: '0.78rem',
    inlineSize: '0.08rem'
}, {
    prefix: prefix
});
