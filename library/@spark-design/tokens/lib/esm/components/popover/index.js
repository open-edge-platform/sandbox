import { popover } from './component';
import { modes } from './modes';
import { properties } from './properties';
export { popover };
export const config = {
    properties,
    component: popover,
    modes
};
